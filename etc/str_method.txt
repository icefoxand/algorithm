.replace() # 문자열을 교체함
.split() # 특정한 구분자를 사용하여, 문자열속 단어들의 리스트를 반환함
.find() # 부분 문자열 sub가, 문자열 속에서 가장 빨리 나오는 인덱스를 반환
.isalpha() # Q. 영어인지 찾는거였나?
.capitalize() # 첫문자만 대문자이고, 나머지는 소문자인 문자열의 복사본을 반환(꾸미기용)
.casefold() # 뭔지모름
.center() # 문자열을 중앙에 두고, 특정문자로 양옆을 채움(꾸미기용)
.count() # 문자열이 등장하는 횟수
